version: '3.8'

services:
  synth-gen:
    build:
      context: .
      dockerfile: docker/Dockerfile.synthetic
    volumes:
      - /mnt/block100:/data
    command:
      - --fonts_dir
      - /data/GoogleFonts
      - --out_dir
      - /data/Synthetic
      - --images_per_font
      - "1000"
